<div local-class="header">
  {{#if (or (media 'isSmall') (media 'isMedium'))}}
    ***Don't forget to change the name of the APP from ember-octane-boilerplate*** 
  {{else}}
    ***Don't forget to change the name of the APP from ember-octane-boilerplate to....whatever it is you named it, simply run a find in the entire app, replace ember-octane-boilerplate with yourAppName or your-app-name.*** 
  {{/if}}
</div>
